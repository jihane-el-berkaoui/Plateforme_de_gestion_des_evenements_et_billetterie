spring:
  application:
    name: booking-service
  
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
    
    discovery:
      enabled: true
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    enabled: true